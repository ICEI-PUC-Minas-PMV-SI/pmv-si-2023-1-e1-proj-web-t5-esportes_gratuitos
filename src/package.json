{
  "name": "projeto-vem",
  "version": "1.0.0",
  "description": "O site em HTML/CSS/JS é um projeto estático, logo pode ser utilizado tanto em servidores...",
  "main": "index.js",
  "scripts": {
    "dev": "cp -r ./pages/env.local.json ./pages/env.json && cd api && node ./server.js",
    "build": "cp -r ./pages/env.prod.json ./pages/env.json && cd api && node ./server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "geolib": "^3.3.4",
    "json-server": "^0.17.3"
  }
}
